import {
} from "./actions";

const initialState = {
  who: "World"
};

export const homeReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
